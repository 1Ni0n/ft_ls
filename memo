Options autorisées: -l -R -a -r -t  
Se rappeler les liens symboliques avec -l, et quand le lien pointe sur qqchose de non valide ou de valide.
Se rappeler que lorsqu'on passe un dossier vide en arg sans options rien ne se passe.
Ne pas oublier que si on fait un chmod 000 . on peut plus faire de ls apres. Ce qui veut dire que si t'essaie de faire un ls su un fichier dans le dossier courant apres avoir mit les perm a 000 un message d'error avec le nom du file s'affiche.
Dans la meme optique que la ligne precedente, checker quel message derreur si on fait un ls . et qu'on a pas les droits.
Verifier que le print list ou autre action sur la liste ne provoque pas de fuite memoire.
Verifier pourquoi on a des blems avec ft_ls /dev et /etc, ya des trucs qui existent pas qui apparraissent et gros vide entre la commande et le debut du retour -> cest bon tavais pas malloc;
Faire gaffe a si on passe des fichiers casses en arg, ils apparaissent mais pas ceux qui sont caches et pas passes en arg donc un trie supplementaire s'impose;
Faire gaffe au dossier munki.
Ne pas oublier que si la liste est vide il faut brake.
Lorsque lon passe juste '-' en arg il le considere pas comme une erreur il devrait. Plus general lorsque juste '-' est passe il le considere pas comme un arg ais comme une opts pas bon.
Lors dun affichage de -l, la section time ne montre pas lannee de modif comme il faudrait sur certains liens symboliques, mais lheure exacte.
Lorsque l'on passe -- il nique;
Lorsqu'on ./ft_ls -lRt ~/ il affiche le total meme si il devrait pas.
./ft_ls -lRrta / segfault.
Affichage d'un total 0 meme sur les dossiers vides en -lRatr racine.
Gerer l'alignement des majmin et de la size des sorties sur un -l.
Le dossier etc est un lien symbolique il doit montrer le lien lorsqu'on le demande sans / a la fin de la commande.


Reprendre au segfault sur ls -lR /dev, segfault sur le /dev/fd/, avec le  dir causes a cycle.

Check_files check_dir main_ls ----> append_to_list;
